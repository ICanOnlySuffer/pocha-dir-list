# translated using Google Translate, may contain errors

# error
path_doesnt_exist   경로를 찾을 수 없습니다.

# usage
usage               용법
options             옵션
printing            인쇄
	printing(a)         대체 버퍼 (루프에서만)
	printing(c)         LS_COLORS의 색상 사용
	printing(d)         지연 (루프에서만)
	printing(l)         루프에
	printing(s)         크기
listing             목록
	listing(d)          디렉토리만
	listing(r)          일반 것들만
	listing(h)          숨겨진 것들만
	listing(a)          모두
sorting             정렬
	sorting(d)          디렉토리 먼저
	sorting(n)          영숫자

# count
directories(0)      디렉토리 0개
directories(1)      디렉토리 1개
directories(2)      디렉토리 2개
directories(+)      %u개의 디렉토리
regular_ones(0)     0 일반
regular_ones(1)     레귤러 1개
regular_ones(2)     레귤러 2개
regular_ones(+)     레귤러 %u개

# version
credits             다음에서 만든 'tree' 을 기반으로 합니다:
copyright           MIT 라이선스에 따라
